<resources>

    <!-- Base application theme. -->
    <style name="PhotoEditorTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pe_colorPrimary</item>
        <item name="colorPrimaryDark">@color/pe_colorPrimaryDark</item>
        <item name="colorAccent">@color/pe_colorAccent</item>
    </style>

    <style name="PhotoEditorTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PhotoEditorTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PhotoEditorTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PhotoEditorEditText">
        <item name="android:singleLine">true</item>
        <item name="colorControlNormal">#FFFFFF</item>
        <item name="colorControlActivated">#FFFFFF</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
